import { staticTextDefault } from '~/config';

export { config, iconfontClassUrl, iconfontUrl, allToolbar, allFooter } from '~/config';

export const zh_CN = staticTextDefault['zh-CN'];
export const en_US = staticTextDefault['en-US'];
